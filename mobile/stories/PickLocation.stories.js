import PickLocation from 'components/ZPickLocation';
import { componentStories } from './utils/vue-stories';

const { definition, story } = componentStories(PickLocation, {});
export default definition;

export const Basic = story();
