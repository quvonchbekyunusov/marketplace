import ZFilterPrice from '@/components/ZFilterPrice';
import { componentStories } from './utils/vue-stories';

const { definition, story } = componentStories(ZFilterPrice);
export default definition;
export const Basic = story();
