import ZCheckCard from '@/components/ZCheckCard';
import { componentStories } from './utils/vue-stories';

const { definition, story } = componentStories(ZCheckCard);
export default definition;

export const Basic = story();
